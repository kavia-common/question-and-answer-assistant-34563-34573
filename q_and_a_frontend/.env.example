# PUBLIC_INTERFACE
# REACT_APP_BACKEND_BASE_URL is used by the frontend to target the backend API.
# Example values:
# - Local dev (if backend on localhost:5000): http://localhost:5000
# - In this environment (backend runs on port 3001): https://vscode-internal-15576-beta.beta01.cloud.kavia.ai:3001
REACT_APP_BACKEND_BASE_URL=
